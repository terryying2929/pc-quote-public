# 公开静态页

这个目录用于 GitHub Pages 调试和发布，只读取同目录下的 `public-data.json`。

使用方式：

1. 登录后台。
2. 点击 `导出公开数据 JSON`。
3. 把下载的文件命名为 `public-data.json`，放到本目录。
4. 将本目录发布到 GitHub Pages。

注意：不要把后台数据库、渠道 Excel、`.env` 或内部接口页面放到 GitHub Pages。
